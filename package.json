{
  "name": "ospedia",
  "description": "OSPedia aims to be a website that brings out the histories of the notable operating systems of the past and present. It appreciates how far we've come with today's technology, how far we're going with tomorrow's technology, and how these systems helped tremendously.",
  "main": ".eleventy.js",
  "scripts": {
    "sass": "sass assets/global/stylesheets:assets/global/stylesheets --no-source-map --style=compressed",
    "sass:watch": "sass assets/global/stylesheets:assets/global/stylesheets --watch --style=compressed",
    "eleventy": "pnpm exec eleventy",
    "dev": "pnpm run sass:watch & pnpm run eleventy --serve",
    "build": "bash -c 'pnpm run sass && pnpm run eleventy && for file in $(find _site -type f | grep -E \"html|css|js\" | grep -E -v \"./pagefind|scss|fluentUI.css\"); do pnpm exec html-minifier-next \"${file}\" -o \"${file}\" --preset comprehensive --collapse-whitespace --remove-redundant-attributes --verbose; done'"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Keyboard1000n17/OSPedia.git"
  },
  "keywords": [],
  "author": "Keyboard1000n17",
  "license": "CC BY-SA 4.0",
  "bugs": {
    "url": "https://github.com/Keyboard1000n17/OSPedia/issues"
  },
  "homepage": "https://github.com/Keyboard1000n17/OSPedia#readme",
  "dependencies": {
    "@11ty/eleventy": "^3.1.2",
    "eleventy-sass": "^3.0.0-beta.0",
    "eruda": "^3.4.3",
    "pagefind": "^1.4.0"
  },
  "devDependencies": {
    "html-minifier-next": "^4.15.2",
    "sass": "^1.97.1"
  }
}
